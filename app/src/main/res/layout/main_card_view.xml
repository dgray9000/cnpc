<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <CheckBox
        android:id="@+id/checkBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:checked="false"
        android:text="@string/delete"
        android:visibility="invisible" />

    <RelativeLayout
        android:id="@+id/linearLayout_cardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/rvTicker_imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="false"
            android:cropToPadding="false"
            android:padding="16dp"
            android:scaleType="fitStart"
            android:contentDescription="@string/symbol_content_description"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true" />

        <TextView
            android:id="@+id/rvTicker_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/rvTicker_imageView"
            android:layout_toLeftOf="@id/rvTicker_switch"
            android:layout_toRightOf="@id/rvTicker_imageView"
            android:layout_toStartOf="@id/rvTicker_switch"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1" />


        <Switch
            android:id="@+id/rvTicker_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:layout_gravity="end" />
    </RelativeLayout>
</android.support.v7.widget.CardView>